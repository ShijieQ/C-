#include<map>
#include<set>
#include<cmath>
#include<queue>
#include<stack>
#include<cstdio>
#include<vector>
#include<cstring>
#include<iostream>
#include<algorithm>

using namespace std;
const int maxn = 100+3;
stirng s, ini;
int p1, p2, p3;

void solve(){

}

int main(){
    cin>>p1>>p2>>p3;
    cin>>ini;
    int len = ini.length();
    s += a[0];
    for(int i = 1; i < len - 1; i++){
        if(a[i] != '-'){
            s += a[i];
        }
        else {
            if(a[i-1] )
        }
    }
}